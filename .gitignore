.DS_Store
*.log
node_modules/
/test-results/
/playwright-report/
/blob-report/
.playwright/

# Claude Code personal (per-user) settings — never ship these
.claude/settings.local.json

# Secrets (R12) — ANTHROPIC_API_KEY is env-only (ADR-001) and must never be committable.
# The obvious local place to put it is a .env file, so make that structurally un-committable.
.env
.env.*
!.env.example
