{
  "name": "calories",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Calories — estimate the calories in a meal from a photo. Node service (frontend + one vision-model API route).",
  "scripts": {
    "start": "node src/server.js",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "vitest": "^2.1.0"
  }
}
