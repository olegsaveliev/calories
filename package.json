{
  "name": "{{PROJECT_SLUG}}",
  "version": "0.4.0",
  "private": true,
  "description": "Dev-only test/lint tooling (web-vanilla preset). The shipped app in src/ has no runtime dependency.",
  "scripts": {
    "lint": "node tools/lint.mjs",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
